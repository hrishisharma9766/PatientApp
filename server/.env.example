# Example .env for the server. DO NOT commit secrets to git.
# Use URL-encoded passwords in the DATABASE_URL (e.g. Hanuman@9766 => Hanuman%409766)

# Direct connection string (URL-encode special characters like @ in the password):
# DATABASE_URL=postgresql://postgres:Hanuman%409766@db.rfgxemqlkylyttjsrxzi.supabase.co:5432/postgres

# OR build from parts (DB_PASSWORD does not need to be encoded when using parts):
DB_HOST=db.rfgxemqlkylyttjsrxzi.supabase.co
DB_USER=postgres
DB_PASSWORD=Hanuman@9766
DB_PORT=5432
DB_NAME=postgres

# Example to set node env
# NODE_ENV=development
# PORT=4000
